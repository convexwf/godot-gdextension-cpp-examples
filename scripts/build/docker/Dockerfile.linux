# Docker image for building GDExtension on Linux target platform
# Uses the same platform name as godot-cpp SCons and .gdextension: "linux"

ARG img_version=4.6
FROM godot-fedora-base:${img_version}

WORKDIR /app

# Install Linux build dependencies
RUN dnf -y install --setopt=install_weak_deps=False \
      gcc \
      gcc-c++ \
      glibc-devel \
      libstdc++-devel \
      libstdc++-static \
      && dnf clean all

CMD /bin/bash
